export const siteConfig = {
  name: "Arretados",
  description: "Torneios de Call of Duty no Brasil",
  url: "https://arretados.gg",
  locale: "pt-BR",
} as const;

export type SiteConfig = typeof siteConfig;
